extends ../layout

block content
  .page-header
    h1.page-title= isEdit ? '✏️ Edit User' : '➕ Add New User'
    a.btn.btn-outline(href="/users") ← Back to Users

  if error && error.length
    each e in error
      .alert.alert-error ⚠️ #{e}

  .section-card
    .section-card-header
      h2= isEdit ? 'Edit User Details' : 'Create New System User'
      if !isEdit
        p.card-subtitle The default password will be the user's phone number. They will be required to change it on first login.
    .section-card-body
      - var formAction = isEdit ? '/users/' + userData.id + '?_method=PUT' : '/users'
      form(action=formAction method="POST")
        if isEdit
          input(type="hidden" name="_method" value="PUT")

        .form-grid
          .form-group
            label Full Name *
            input(type="text" name="fullName" value=userData.fullName || '' placeholder="e.g. John Mwangi" required autofocus)

          .form-group
            label Email Address
            input(type="email" name="email" value=userData.email || '' placeholder="user@example.com")

          .form-group
            label Phone Number *
            input#phoneInput(type="text" name="phoneNumber" value=userData.phoneNumber || '' 
              placeholder="e.g. 0712345678" required)
            if !isEdit
              small.field-hint This will be used as the default password

          .form-group
            label Role *
            select(name="role" required)
              option(value="") — Select Role —
              option(value="admin" selected=(userData.role === 'admin')) 👑 Admin
              option(value="data_entry" selected=(userData.role === 'data_entry')) ✏️ Data Entry

          if !isEdit
            .form-group
              label Default Password (Auto-set)
              input#defaultPwdDisplay(type="text" value="" placeholder="Will be set to phone number" disabled)
              small.field-hint Read-only — automatically set to the phone number entered above

          if isEdit
            .form-group
              label Account Status
              select(name="isActive")
                option(value="1" selected=(userData.isActive)) Active
                option(value="0" selected=(!userData.isActive)) Inactive

          if isEdit
            .form-group
              label Auto-generated Username
              input(type="text" value=userData.username disabled)
              small.field-hint Username cannot be changed

        .form-actions
          button.btn.btn-primary.btn-lg(type="submit")
            = isEdit ? '💾 Save Changes' : '➕ Create User'
          a.btn.btn-outline(href="/users") Cancel

block scripts
  if !isEdit
    script.
      const phoneInput = document.getElementById('phoneInput');
      const pwdDisplay = document.getElementById('defaultPwdDisplay');
      phoneInput.addEventListener('input', function() {
        pwdDisplay.value = this.value ? this.value : '';
      });
